apiVersion: v1
kind: Service
metadata:
  name: {{ include "member-consumer-ids.fullname" . }}
  labels:
    {{- include "member-consumer-ids.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "member-consumer-ids.selectorLabels" . | nindent 4 }}
