apiVersion: v1
kind: Service
metadata:
  name: {{ include "member-consumer-address.fullname" . }}
  labels:
    {{- include "member-consumer-address.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "member-consumer-address.selectorLabels" . | nindent 4 }}
